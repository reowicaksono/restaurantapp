const URL_IMAGE = 'https://restaurant-api.dicoding.dev/images/small/';
const BASE_URL_API = 'https://restaurant-api.dicoding.dev/';
const tableName = 'favorites';

// enum ResultState { loading, noData, hasData, error }
